* 수정, 삭제는 같은 url이지만 method를 통해 기능을 구분한다.
* page_size와 page_no에 따라 db 쿼리에서 받은 데이터를 몇 개로 나눌 지 계산 하고
* 나눈 데이터의 몇 번째 덩어리를 결과로 보낼 지 결정해서 response 시킨다.
* job/list 의 offer_id만으로 호출 할 경우 구인자가 자기가 등록한 건수를 볼 수 있다.
* url에 id 값 처럼 유동적으로 변할 수 있는 경우, 그 값을 가장 마지막에 두는 경우가 흔하다.
* 14. applyer-list에서 지원자를 조회했을 때 taker_id까지 받아와서 그 taker_ID를 17번에 넣어 그 값을 가져올 수 있다.
* 회원 정보 조회의 경우 구직자 또한 구인자의 정보를 알아야하니.
* role은 해당 session 사용자.


